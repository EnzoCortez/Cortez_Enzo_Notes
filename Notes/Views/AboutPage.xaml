<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:Notes.ViewModels"
             x:Class="Notes.Views.AboutPage">
    <ContentPage.BindingContext>
        <viewModels:AboutViewModel />
    </ContentPage.BindingContext>

    <Frame BackgroundColor="LightGray" CornerRadius="10" Padding="10" Margin="10">
        <VerticalStackLayout Spacing="10" HorizontalOptions="Center">
            <Label Text="Autor: Enzo Cortez" FontSize="20" FontAttributes="Bold" HorizontalOptions="Center" />
            <Label Text="Carrera: Ingeniería en Software" FontSize="18" HorizontalOptions="Center" />
            <Image Source="images.jpg" 
                        />
        </VerticalStackLayout>
    </Frame>


</ContentPage>